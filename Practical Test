#include <stdio.h>

int main() {
    int sum = 0;

    // Loop through numbers less than 5
    for (int i = 0; i < 5; i++) {
        sum += i; // Add the current number to the sum
    }

    printf("The sum of numbers less than 5 is: %d\n", sum);
    return 0;
}
